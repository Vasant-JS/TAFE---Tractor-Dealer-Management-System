{
  "name": "tafe-dms-render-root",
  "version": "1.0.0",
  "private": true,
  "description": "Render root entrypoint for the TAFE Dealer Management System backend.",
  "scripts": {
    "postinstall": "cd backend && npm install && npm run prisma:generate && npm run build",
    "start": "cd backend && npm run prisma:push && node dist/prisma/seed.js && npm run start"
  },
  "engines": {
    "node": "24.x"
  }
}
